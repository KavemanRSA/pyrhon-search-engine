# pyrhon-search-engine
A search engine in Python using TF-IDF indexing, stopword removal, and stemming. It builds a term dictionary, a document dictionary, and supports querying over a document corpus.
